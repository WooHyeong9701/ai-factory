# Copy this file to .env and adjust as needed.
#
# VITE_API_BASE_URL: URL of the AI Factory backend (FastAPI server).
# Each user runs the backend locally on their own machine.
# Default: http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
