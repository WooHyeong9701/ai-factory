# Copy this file to .env and adjust as needed.

# AI Factory backend (FastAPI, runs locally on user's machine)
VITE_API_BASE_URL=http://localhost:8000

# Cloudflare Worker URL — workflow save/load (deploy cf-worker/ first)
# Example: https://ai-factory-api.your-account.workers.dev
VITE_CF_WORKER_URL=
